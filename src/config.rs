use crate::*;

pub const MAP_FILE: &str = "map.png"; // Chemin vers le fichier de la map
pub const BMP_FILE: &str = "assets/provinces.bmp"; // Chemin vers le fichier BMP contenant les provinces
pub const PROVINCE_BORDER_COLOR: Color = Color::srgb(250.0, 200.0, 0.0);
